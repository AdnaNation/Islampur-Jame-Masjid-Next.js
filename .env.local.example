# MongoDB Atlas credentials (used to build the connection URI in lib/mongodb.js)
DB_USER=
DB_PASSWORD=

# Secret used to sign/verify JWTs
ACCESS_TOKEN_SECRET=

# SMS provider (mimsms) credentials, used by /api/check-balance and /api/sms
API_USERNAME=
API_KEY=
